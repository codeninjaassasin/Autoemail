node_modules/
.env
.env.*
!.env.example
*.env
data/
.playwright-mcp/

# WireGuard/OpenVPN configs carry a PrivateKey that grants access to the
# account. Both spellings — macOS is case-insensitive, so the directory can be
# committed under either.
proxies/
Proxies/
*.conf
*.ovpn
